on:
  push:
    branches:
      - main

jobs:
  backend:
    runs-on: [self-hosted, edumosaic-backend]  

    steps:

      

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js 
        uses: actions/setup-node@v3
        with:
          node-version: '23.7.0'

      - name: Pull and Deploy Backend
        working-directory: /var/www/projects/eq-aditya-j-mern-stack-internship-2025/backend/
        run: |
          echo "Pulling latest code for backend" 
        
          
          git pull 
          npm install &&
          pm2 restart index.js --name "edumosaic-backend"
